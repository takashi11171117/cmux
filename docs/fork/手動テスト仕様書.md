# Agent-first IDE 手動統合テスト仕様書

自動テストで届かない部分だけを列挙する。ユニットテストは 130 件通っており、値型・ポリシー・
コーディネータ・エンジン・行インデックスの正しさはそこで担保されている。ここに書くのは
**実際に描画してみないと分からないこと**と、**複数の入口をまたぐ振る舞い**に絞る。

## 変更サマリー

| カテゴリ | 内容 |
|---|---|
| 対象ブランチ | `fork/agent-first-ide` |
| ベース | `5734a451cc`（upstream/main の1コミット手前） |
| コミット数 | 15（うちコード変更は5） |
| 変更ファイル | 52（`Sources/` / `Packages/` / `cmuxTests/` / `web/` / `Resources/`） |
| 主な変更 | プレーンテキストエディタにシンタックスハイライトと行番号を追加。外部変更と未保存編集が衝突したときに Reload / Keep Mine を提示。外部エディタへ行番号を渡せるようにした |

## 事前準備

```bash
./scripts/reload.sh --tag agent-first-ide --launch
```

テスト用ファイルを用意しておく。

```bash
mkdir -p /tmp/afide-manual && cd /tmp/afide-manual
printf 'import Foundation\n// line comment\nlet greeting = "hello"\nlet count = 42\n/* block\n   comment */\n' > sample.swift
printf 'const x: number = 1; // note\n' > sample.ts
printf 'void main() { var x = 1; }\n' > sample.dart
printf 'no known grammar here\n' > sample.foo
python3 -c "print('x = 1\n' * 200000)" > huge.py          # 約 1.2 MB
python3 -c "print('y = 2\n' * 30000)" > big.py            # 約 180 KB
```

> **⚠️ テスト中はユーザーの本番 cmux を終了しておくこと。** タグ付きビルドは別バンドル ID を持つが、
> テストで起動する untagged な `cmux DEV.app` が既定のデバッグソケットとセッションファイルを共有する。

---

## 🔴 必須（ここが壊れていたらマージしない）

### A. ハイライトの基本描画

- [ ] **A-1 色が出る**: `/tmp/afide-manual/sample.swift` を File Explorer からダブルクリック → `import` / `let` がキーワード色、`"hello"` が文字列色、`// line comment` がコメント色、`42` が数値色で、いずれも本文色と**異なる**
- [ ] **A-2 ブロックコメントが最後まで色付く**: 同ファイルの `/* block` から `comment */` まで**2行にまたがって**コメント色になっている（1行目だけ色が付いて2行目が素になっていたら失敗）
- [ ] **A-3 未対応拡張子はプレーン**: `sample.foo` を開く → 色は付かないが、**本文は欠落せず全部表示され、編集もできる**
- [ ] **A-4 Dart も色が付く**: `sample.dart` を開く → `void` / `var` がキーワード色、`1` が数値色になる。同梱ビルドに dart 文法が無いため**別ファイルを vendoring して読み込んでいる**箇所で、ここが色付かない場合は文法の読み込みに失敗している

### B. テーマ連動（最も壊しやすい箇所）

- [ ] **B-1 ライト/ダーク両方で読める**: `sample.swift` を開いたまま、システム外観をライト→ダーク→ライトと切り替える → **再起動なしで**配色が変わり、どちらでも全トークンが背景に対して判読できる
- [ ] **B-2 色が塗り潰されない**: `sample.swift` を開いたまま、**別パネルをクリックする / タブを切り替えて戻る / ウィンドウをリサイズする**を数回繰り返す → ハイライトが本文色一色に戻らない
  - これは `applyTheme` が SwiftUI 更新のたびに `textView.textColor` を全体へ代入する挙動を回避できているかの確認。**回帰しやすい最重要項目**
- [ ] **B-3 スクロール先も色が付いている**: 大きめのファイル（`big.py`）を開き、**一気に最下部までスクロール** → 到達した領域が黒一色（ダークテーマなら読めない状態）になっていない

### C. 保存衝突（唯一の既存挙動変更）

- [ ] **C-1 バナーが出る**: `sample.swift` を開いて**保存せずに1文字打つ** → 別ターミナルで `echo "changed" >> /tmp/afide-manual/sample.swift` → **「This file changed on disk」バナーが出る**
- [ ] **C-2 Keep Mine**: バナーで Keep Mine → バナーが消え、**エディタの内容は自分の編集のまま**、未保存マークも残る → Cmd+S で保存するとディスク側が自分の内容で上書きされる
- [ ] **C-3 Reload**: 同じ状況を作り直して Reload → **エディタの内容がディスク側に置き換わり**、未保存マークが消える
- [ ] **C-4 クリーンなら出ない**: 何も編集していない状態で外部から書き換える → **バナーは出ず**、内容が自動で更新される（従来どおり）
- [ ] **C-5 自分の保存では出ない**: 編集して Cmd+S → **バナーが出ない**（自分の保存がファイル監視経由で返ってくるだけなので）
- [ ] **C-6 ファイルが消えたらバナーも消える**: バナーが出ている状態で `rm /tmp/afide-manual/sample.swift` → バナーが消え、ファイル無しの表示になる

### D. 行番号

- [ ] **D-1 番号と本文が揃う**: `sample.swift` を開く → 左端に 1 から始まる行番号が出て、各行と縦位置が一致
- [ ] **D-2 折り返しでも1行1番号**: 設定で `fileEditor.wordWrap` をオンにし、横幅を超える長い行を含むファイルを開く → **折り返された2行目以降には番号が出ない**
- [ ] **D-3 ズームで崩れない**: ⌘+ / ⌘- を数回、およびトラックパッドのピンチ → 行番号の**文字サイズも一緒に変わり**、本文との縦位置が一致し続ける
- [ ] **D-4 桁が増えても切れない**: `huge.py`（20万行）を開いて最下部へ → 6桁の行番号が表示され、**本文が水平に切れていない**

---

### I. コードレビューカラム

カラムは専用の `Workspace` を `WorkspaceContentView` で描いている。つまりタブ・分割・ブラウザは
Bonsplit の既存実装がそのまま動くはずで、ここで確認するのは「独自実装に化けていないこと」。

- [ ] **I-1 ファイルはカラムに開く**: File Explorer でファイルをダブルクリック → **ターミナルのペインは分割も縮小もされず**、右隣のカラムにファイルが開く
- [ ] **I-2 2つ目以降はタブとして増える**: 続けて別のファイルを開く → カラムが**分割されず**、カラム内のタブが増える。ターミナル側のパネルには**開かない**
- [ ] **I-3 同じファイルを開いても増えない**: 既に開いているファイルをもう一度開く → タブは増えず、そのタブが選択される
- [ ] **I-4 カラム内で分割できる**: カラムのタブバーの分割ボタン（`▯▯` / `⊟`）を押す → **カラムの中だけ**が2分割され、ターミナル領域は動かない
- [ ] **I-5 カラム内でブラウザが開く**: カラムのタブバーの `🌐` を押す → カラム内にブラウザが開く
- [ ] **I-6 新規ターミナルボタンは無い**: カラムのタブバーに `>_`（新規ターミナル）が**無い**。ターミナル側のタブバーには**ある**
- [ ] **I-7 閉じる**: カラム右上の閉じるボタンを押す → カラムが消え、ターミナルが広がる
- [ ] **I-8 開き直すと中身が残っている**: ターミナル側タブバーの `</>` を押す → カラムが再表示され、**閉じる前に開いていたタブがそのまま**残っている
- [ ] **I-9 幅を変えられる**: カラムとターミナルの境界をドラッグ → 幅が変わる。**極端に狭くしても 320pt より狭くならず**、広くしてもウィンドウの 60% を超えない
- [ ] **I-10 tree の開閉**: ターミナル側タブバーの `▯` を押す → 右端の tree が消える／再度押すと戻る

### J. ファイルツリーの右クリックメニュー

書き込み系は区切り線の下にまとまっている。**線より上の項目はファイルを一切変更しない**という
前提が崩れていないかを見る。

- [ ] **J-1 項目が出る**: tree でファイルを右クリック → 「コードレビューで開く」「ここで新規ターミナル」「Finder で表示」「パスをコピー」「相対パスをコピー」「ファイル名をコピー」、区切り線を挟んで「新規ファイル…」「新規フォルダ…」「名前を変更…」「複製」「ごみ箱に入れる」が出る
- [ ] **J-2 フォルダでは「コードレビューで開く」が出ない**: フォルダを右クリック → その項目だけ無い（他は出る）
- [ ] **J-3 コードレビューで開く**: ファイルで実行 → カラムにそのファイルが開く
- [ ] **J-4 ここで新規ターミナル**: **フォルダ**で実行 → そのフォルダを作業ディレクトリにターミナルが開く（`pwd` で確認）。**ファイル**で実行 → その**親フォルダ**で開く
- [ ] **J-5 ファイル名をコピー**: 実行 → パスではなく**ファイル名だけ**がクリップボードに入る
- [ ] **J-6 新規ファイル**: 実行 → 名前を聞かれ、入力すると**そのフォルダ配下に**空ファイルができ、tree に現れる
- [ ] **J-7 既存名は拒否される**: 既にある名前で新規ファイル → 「すでに存在します」と出て、**元のファイルの中身が消えていない**
- [ ] **J-8 名前を変更**: 実行 → 現在の名前が入力欄に入っている状態で聞かれ、変更すると tree に反映され、**中身が保たれている**
- [ ] **J-9 複製**: ファイルで実行 → `名前 copy.拡張子` ができる。もう一度実行 → `名前 copy 2.拡張子` になる
- [ ] **J-10 ごみ箱**: 実行 → **確認ダイアログが出る**。OK すると tree から消え、**Finder のごみ箱に入っている**（完全削除されていない）
- [ ] **J-11 キャンセルで何も起きない**: J-10 の確認ダイアログでキャンセル → ファイルが残る
- [ ] **J-12 VS Code はプロジェクトごと開く**: リポジトリ内のファイルを右クリック → 「Visual Studio Code で開く」 → **ウィンドウのタイトルが「ファイル名 — リポジトリ名」** になり、サイドバーにプロジェクトのツリーが出る（ファイル1枚だけではない）
- [ ] **J-13 Cursor でも同じ**: 同じ操作を Cursor で → プロジェクトごと開く
- [ ] **J-14 エディタ以外は巻き込まない**: 画像を右クリック → 「プレビューで開く」 → **画像だけが開く**（フォルダが Finder 等で開かれたりしない）
- [ ] **J-15 monorepo の一部を開いている場合**: リポジトリのサブディレクトリをワークスペースのルートにしている状態で J-12 → **リポジトリ全体ではなくそのサブディレクトリ**が開く
- [ ] **J-16 リポジトリを束ねたフォルダを開いている場合**: 複数リポジトリを含む親フォルダをルートにした状態で J-12 → **親フォルダではなくファイルが属するリポジトリ**が開く
- [ ] **J-17 git 管理外**: git 管理外のフォルダ配下のファイルで J-12 → ワークスペースのルートが開く。ルート外のファイルならファイル単体で開いて壊れない

### K. Git タブ（右サイドバー）

- [ ] **K-1 タブが出る**: 右サイドバーで Files の隣に Git タブがある
- [ ] **K-2 一覧が出る**: リポジトリのフォルダを開くと変更ファイルが変更種別つきで並ぶ。色はツリーの変更マークと同じ
- [ ] **K-3 自動更新**: ファイルを保存すると数秒以内に一覧へ反映される。別ターミナルで `git add` しても反映される
- [ ] **K-4 行クリック**: 行をクリック → そのファイルの差分がコードレビューカラムに開き、**中身（赤緑の行）が描画される**
- [ ] **K-5 未追跡ファイル**: `?` の行をクリック → 全行追加の差分として出る
- [ ] **K-6 ヘッダーの `</>`**: 全体差分がカラムに開く
- [ ] **K-7 タブが増えない**: 差分を何度開いても差分タブは1枚のまま、ターミナルも分割されない
- [ ] **K-8 `cmux diff` の既定は不変**: 端末で `cmux diff --unstaged` → 従来どおり右に分割される
- [ ] **K-9 カラムのブラウザ**: カラムの `🌐` で開いたページが描画され、スクロール・クリックが効く
- [ ] **K-10 非リポジトリ**: Git 管理外のフォルダを開くと「変更なし」表示でエラーにならない
- [ ] **K-11 ステージ済みが開く**: `git add <ファイル>` してから、そのファイルの行をクリック → 差分が開く（これが開かないのが元のバグ）
- [ ] **K-12 ステージ+未ステージの混在**: `git add` した後にさらに同じファイルを編集 → 行クリックで **両方の変更が1つの差分に出る**
- [ ] **K-13 初コミット前**: `git init` 直後のフォルダを開き、ファイルの行をクリック → 全行追加として差分が出る
- [ ] **K-14 ツリーになっている**: 一覧がフォルダごとに畳まれ、フォルダが同階層のファイルより上に並ぶ
- [ ] **K-15 畳める**: フォルダ行をクリック → 閉じて中身が隠れ、右の数字は中の変更ファイル数のまま。もう一度クリックで開く
- [ ] **K-16 単独フォルダの圧縮**: 中に1つしかフォルダが無い階層が `docs/fork/git` のように1行にまとまる
- [ ] **K-17 畳んだ状態が保たれる**: フォルダを畳む → Files タブへ切り替えて Git タブへ戻る → 畳んだままになっている
- [ ] **K-18 畳んだ後の更新**: フォルダを畳んだ状態でファイルを保存 → 一覧が更新されても畳んだ状態が壊れない

### L. History タブ（右サイドバー）

- [ ] **L-1 タブが出る**: 右サイドバーの Git の隣に「履歴」タブが出る
- [ ] **L-2 一覧が出る**: リポジトリのフォルダを開くと、コミットが新しい順に並ぶ。行に**短縮ハッシュ・件名・作者・相対日時**
- [ ] **L-3 ページ継ぎ足し**: 下までスクロール → 続きが継ぎ足され、位置は飛ばない。数万コミットのリポジトリでも開いた瞬間に固まらない
- [ ] **L-4 コミット後に追随**: 別ターミナルで `git commit` → 数秒以内に一覧の先頭に現れる
- [ ] **L-5 ファイル保存では読み直さない**: ファイルを保存しただけでは `git log` が走らない（HEAD が動いていないため）
- [ ] **L-6 ブランチ切替**: `git checkout` で別ブランチへ → 履歴が入れ替わる
- [ ] **L-7 行クリックでカラムに差分**: 行をクリック → コードレビューカラムにそのコミットの差分が開き、赤緑の行が描画される
- [ ] **L-8 差分タブは1枚のまま**: 別のコミット行をクリック → **同じタブが差し替わる**（タブが増えない）
- [ ] **L-9 マージコミット**: 親が2つあるマージコミットの行をクリック → 差分が出る（`--first-parent` の効果）
- [ ] **L-10 ルートコミット**: 親を持たないコミットで L-7 → 差分が出る
- [ ] **L-11 ターミナルが分割されない**: L-7 の実行前後で、既存のターミナルペインの数が変わらない（環境変数 scrub の効果）
- [ ] **L-12 リポジトリ切替**: ワークスペースを別のリポジトリに切り替える → 履歴が入れ替わる（前の SHA が残らない）
- [ ] **L-13 git 管理外**: git 管理外のフォルダを開くと「履歴なし」表示でエラーにならない
- [ ] **L-14 CLI からも切替可**: `cmux right-sidebar set history` でタブが切り替わる
- [ ] **L-15 グラフを開く**: History タブヘッダーの `</>` → コードレビューカラムにグラフが開く。左端にレーンとドット、ドットの隣に**ブランチ・タグ badge**、そして短縮ハッシュ・件名・作者・日時
- [ ] **L-16 マージが分岐して見える**: マージコミットは**中抜きの丸**、下に線が2本に分かれてから合流する
- [ ] **L-17 グラフタブは1枚のまま**: L-15 を2回押しても差分ビューアと同じくタブが増えない（同じファイルURLを上書き）
- [ ] **L-18 別リポジトリでは別ファイル**: 別のリポジトリを開いて L-15 → **別ワークスペースなので別ファイル**が開き、前のグラフは残っている

### M. Git のステージ操作（右サイドバー Git タブ）

準備: `git init` したフォルダに、①`git add` 済みのファイル ②編集だけのファイル ③`git add` 後にさらに編集したファイル ④未追跡ファイル ⑤サブフォルダ内の編集 を用意する。

- [ ] **M-1 2 セクション**: 「ステージ済みの変更」と「変更」に分かれて並ぶ。①は上、②④⑤は下
- [ ] **M-2 両方にあるファイル**: ③が両セクションに出る。上の行クリック → `git add` 時点の差分だけ、下の行クリック → その後の編集だけ
- [ ] **M-3 件数**: ヘッダの「N 件変更」はファイル数（③は 1 件）。各セクションの数字はフォルダを畳んでも減らない
- [ ] **M-4 hover でボタン**: 「変更」の行に hover → `↺` `+`、「ステージ済み」の行に hover → `−`。hover を外すと消える
- [ ] **M-5 `+`**: 「変更」の行で `+` → 数秒以内に「ステージ済み」へ移る。Files ツリーの色も更新される
- [ ] **M-6 `−`**: 「ステージ済み」の行で `−` → 「変更」へ戻る
- [ ] **M-7 初コミット前の `−`**: `git init` 直後に `git add` したファイルで `−` → `?`（未追跡）に戻り、ファイル本体は残る
- [ ] **M-8 `↺` の確認**: 「変更」の行で `↺` → 確認ダイアログ。**Return を押しても破棄されない**（Cancel が既定）
- [ ] **M-9 `↺` 追跡済み**: Discard → ファイルが `git add` 時点（または最後のコミット）の内容に戻る。③なら「ステージ済み」側の行は残る
- [ ] **M-10 `↺` 未追跡**: Discard → ファイルがごみ箱に入る（Finder のごみ箱で確認、`rm` されていない）
- [ ] **M-11 ボタンは行クリックにならない**: `+` `−` `↺` を押しても差分カラムが開かない
- [ ] **M-12 フォルダの偽行が無い**: ⑤で `src` フォルダ行の隣に `M src` のようなファイル行が出ない
- [ ] **M-13 未追跡フォルダ**: 新しいフォルダに複数ファイルを置く → ファイルごとに `?` 行が出る（`dir/` 1 件ではない）
- [ ] **M-14 「ファイル」タブ**: Git タブ表示中に「ファイル」を押す → 切り替わる（タイトルバー帯のはみ出し修正）
- [ ] **M-15 差分ビューアのソース切替**: 端末で `cmux diff --unstaged` → ビューア右上で Staged を選ぶ → 描画される（"Could not render" にならない）
- [ ] **M-16 コンフリクト中**: マージ衝突中のファイルは「変更」に 1 回だけ出る
- [ ] **M-17 SSH**: SSH 接続のフォルダでは行は出るが `+` `−` `↺` は反応せず、差分も開かない（未対応）

## 🟡 推奨

### E. 設定のライブ反映

- [ ] **E-1 ハイライト切替**: エディタを開いたまま Settings → App → Syntax Highlighting をオフ → **再起動なしで**色が消え、本文がテーマの前景色になる → オンに戻すと色が戻る
- [ ] **E-2 行番号切替**: 同様に Line Numbers をオフ → ルーラが消える → オンで戻る
- [ ] **E-3 cmux.json から**: `~/.config/cmux/cmux.json` に `"fileEditor": { "syntaxHighlight": false, "lineNumbers": false }` を書いて保存 → 設定画面のトグルが両方オフになる
- [ ] **E-4 コマンドパレット**: ⌘K で "Syntax Highlighting" / "Line Numbers" を検索 → 出てきて、選ぶとトグルできる
- [ ] **E-5 設定検索**: Settings の検索欄に `syntax` / `line numbers` / `fileEditor.syntaxHighlight` → 該当行にジャンプする

### F. 入口別のルーティング

同じファイルを3つの入口から開き、**どれも同じように色と行番号が出る**ことを確認する。

- [ ] **F-1 File Explorer**: ダブルクリック
- [ ] **F-2 ターミナル Cmd-click**: `echo /tmp/afide-manual/sample.swift` して出力を Cmd-click
- [ ] **F-3 CLI**: `CMUX_TAG=agent-first-ide scripts/cmux-debug-cli.sh` 経由で `cmux open /tmp/afide-manual/sample.swift`
  - **`/tmp/cmux-cli` は使わないこと**（ユーザーの本番アプリのソケットを叩く恐れ）
- [ ] **F-4 新規拡張子**: `.dart` / `.php` / `.mjs` / `.cjs` / `.cxx` / `.hh` の各ファイルを開く → **QuickLook を経由せず**最初からテキストとして開く（一瞬プレビュー表示が挟まったら失敗）

### G. 外部エディタへの行番号（AFIDE-14）

前提: Settings で `app.preferredEditor` に行番号付きコマンドを設定する。VS Code なら `code --goto {file}:{line}`。

- [ ] **G-1 指定行で開く**: `sample.swift` の3行目にカーソルを置く → ヘッダの共有メニュー → **「Open in Editor at Line 3」** → VS Code が3行目にカーソルを置いて開く
- [ ] **G-2 プレースホルダ無しでも壊れない**: `app.preferredEditor` を `code` だけにする → メニュー項目は出るが、ファイルが（行指定なしで）普通に開く
- [ ] **G-3 テキスト以外では出ない**: PNG や PDF を開く → **「Open in Editor at Line N」項目が出ない**
- [ ] **G-4 行番号オフでも動く**: Line Numbers 設定をオフにする → G-1 が**同じ行番号で**動く（カーソル行は行番号ルーラとは独立に計算されるため）
- [ ] **G-5 既存の「Open in <App>」が無事**: 同じメニューの従来項目からも、これまでどおり開く

### H. Markdown パネル

- [ ] **H-S1 コードビューが下端まで進む**: md を開き「TextEdit を表示」へ切り替え、**折り返しオン・ハイライトオン**でホイールを回し続ける → 途中で引き戻されず最後まで進む
- [ ] **H-S2 折り返しオフでも同じ**: 折り返しをオフにして H-S1 → 同じく最後まで進む
- [ ] **H-S3 色は付いている**: H-S1 の状態でキーワードや文字列に色が付いている（スクロールした先も）
- [ ] **H-S4 ハイライトを切ると色が消える**: 設定でハイライトをオフ → 本文がテーマの文字色一色になり、前の色が残らない
- [ ] **H-S5 テーマ変更で色が入れ替わる**: テーマを切り替える → 前のテーマの色が残った範囲がない
- [ ] **H-1 テキスト編集モードでも色が付く**: `.md` を Markdown ビューアで開き、テキスト編集モードに切り替える → ハイライトと行番号が出る
- [ ] **H-2 Markdown の保存衝突**: Markdown のテキスト編集モードで未保存編集を作り、外部から書き換える → バナーが出て Reload / Keep Mine が効く

---

## 🟢 任意（性能・大規模）

- [ ] **I-1 大きいファイルでも即座に開く**: `huge.py`（1.2 MB / 20万行）を開く → **開くこと自体は待たされない**（色は後から付いてよい）
- [ ] **I-2 サイズ上限でプレーンに落ちる**: 1.5 MB を超えるソースファイルを作って開く → 色は付かないが本文は全部出て編集できる
- [ ] **I-3 タイピングが引っかからない**: `big.py`（18万文字）で連続入力 → **文字が取りこぼされない**、カーソルが飛ばない
- [ ] **I-4 スクロールが重くならない**: `huge.py` でスクロールバーを掴んで上下に激しく動かす → 数百 ms 単位の引っかかりが出ない
  - 設計上ここが最も怪しい。エンジンはテキスト変更時のみ動き、スクロールはキャッシュ再描画だけのはずだが、実測していない

---

## リグレッション確認（既存機能を壊していないこと）

- [ ] **R-1 保存**: 編集 → Cmd+S → ディスクに反映され、未保存マークが消える
- [ ] **R-2 Undo / Redo**: 数文字打って Cmd+Z → **1回の Undo で打った文字だけが戻る**（色が戻るのではなく）。Cmd+Shift+Z で redo
- [ ] **R-3 検索**: Cmd+F で検索、置換 → 従来どおり動く
- [ ] **R-4 ワードラップ**: `fileEditor.wordWrap` のオン/オフ → 従来どおり切り替わる
- [ ] **R-5 フォントズーム**: ⌘+ / ⌘- / ピンチ → 本文サイズが変わる
- [ ] **R-6 外部変更の自動反映**: クリーンな状態で外部から書き換え → 自動で内容が更新される
- [ ] **R-7 画像・PDF・メディア**: `.png` / `.pdf` / `.mp4` を開く → 従来どおりのビューアで開く（テキストとして開いてしまわない）
- [ ] **R-8 Session Restore**: Terminal / Browser / Markdown / File Preview / Project を含む workspace を作る → アプリを終了 → 再起動 → **パネル構成・分割・選択が復元される**
  - 最優先の制約（NFR-01）。セッションのスキーマは1バイトも変えていないので通るはずだが、実機確認は必須

---

## テスト優先度

| 優先度 | 項目 | 理由 |
|---|---|---|
| 🔴 必須 | B-2（色が塗り潰されない） | `applyTheme` は SwiftUI 更新のたびに走る。ここの回避が崩れると、一見動いているのに操作した瞬間に色が消える。ユニットテストでは1パスしか再現できない |
| 🔴 必須 | C-1〜C-6（保存衝突） | **唯一の既存挙動変更**。誤検知すると保存のたびにバナーが出て邪魔になり、検知漏れすると編集内容を失う |
| 🔴 必須 | R-8（Session Restore） | NFR-01 が最優先の制約。壊すと全ユーザーの作業状態が失われる |
| 🔴 必須 | B-3（スクロール先の色） | 未描画領域が `NSTextView` 既定の黒になると、ダークテーマで**読めなくなる** |
| 🟡 推奨 | D-3（ズーム同期） | 実装中にキャッシュ由来のバグを1件踏んでいる箇所 |
| 🟡 推奨 | F-1〜F-4（入口別） | Shared behavior policy の要求。1入口だけ直して他が漏れる典型 |
| 🟡 推奨 | G-1〜G-5（外部エディタ） | 新規-G の判断が妥当だったかの実機確認 |
| 🟢 任意 | I-1〜I-4（性能） | 数値目標が未確定（未確定-08）。体感で判断するしかない |

---

## 既知の未達・注意点

| 項目 | 状態 |
|---|---|
| **Dart のハイライト** | **解決済み。** 同梱ビルド（36言語）に dart が無かったため、同一 11.10.0 の `dart.min.js` を `Resources/markdown-viewer/` へ vendoring した。同ディレクトリは pbxproj 上 folder reference なのでプロジェクトファイルに差分は出ない |
| **Compare ボタン** | **意図的に無い。** FR-07 は 2026-08-17 に2択で確定（未確定-03 解決済み）。`.patch` にディスク側と未保存バッファが平文で載るため、補助機能に対してコストが見合わないと判断 |
| **性能の数値目標** | 未確定-08 のまま。I-1〜I-4 は体感判断 |
| **中間輝度テーマ** | 新規-L 未解決。同梱 CSS は `#ffffff` / `#0d1117` 前提なので、中間的な背景色のカスタムテーマでのコントラストは保証されない |
| **upstream 追従** | 保留中。upstream `main` は #10225 により macOS でビルド不能 |
| **フルテストスイート** | ローカルでは `AppDelegateShortcutRoutingTests` でハングする。緑判定は `scripts/verify-remote.sh mac --tag agent-first-ide` か CI に委ねる |

## テスト後にやること

- [ ] 気づいた不具合を、**どの項目で / 何をしたら / 何が起きたか**の形で記録する
- [ ] 性能（I-1〜I-4）で引っかかりを感じたら、ファイルサイズと操作を添えて記録する。未確定-08 の数値目標を決める材料になる
