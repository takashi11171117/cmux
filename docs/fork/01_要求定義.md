# cmux fork: Agent-first IDE 要求定義

> [!note] このディレクトリについて
> `docs/fork/` は **この fork（takashi11171117/cmux）独自のドキュメント**を置く場所。upstream (`manaflow-ai/cmux`) には存在しない。
> upstream 追従時の衝突を避けるため、fork 独自の変更はこのディレクトリと専用ブランチに閉じる。
>
> 本書の出自: 外部ツール（ChatGPT）が生成した実装指針を、Claude が実リポジトリと GitHub API で検証・訂正したもの。
> **訂正箇所は `> 🔴 訂正` として明示**し、根拠を `[実コード: path:line]` / `[PR: 番号]` / `[GitHub API]` で示す。訂正のない節は原文のまま（＝未検証）。
>
> **検証時点**
> - 初回検証: 2026-08-16（`6d9e1989f` 以前の HEAD）。`> 🔴 訂正` ブロックの大半はこの時点のもの
> - 訂正の再検証: 2026-08-17 / `0ce49c271` 時点。**§6 / §12 / §14 / §25 の訂正自体に誤りが見つかり差し替えた**（各節に「再検証」として明記）
> - `[GitHub API]` の数値（upstream の PR 件数、fork の遅れ）は **2026-08-16 時点の観測**であり、時間とともに変わる。判断の再利用時は再取得すること

---

## 0. 検証サマリ（Claude 追記）

原文は「cmux に既にこれらの機能があるから再実装しない」という前提で組まれている。その前提を実コードで検証した結果、**前提はおおむね正しいが、残作業の見積もりと upstream 戦略が実態と合っていない**。

### 結論1: 残差分は「シンタックスハイライト」と「行番号」の2つだけ

cmux HEAD には既に NSTextView ベースの編集・保存機能がある `[実コード: Sources/Panels/FilePreviewTextEditor.swift, 477行]`。MarkdownPanel と FilePreviewPanel が `FilePreviewTextEditingPanel` を実装して共有している `[実コード: Sources/Panels/MarkdownPanel.swift:16]`。

| §13 の MVP 項目 | 実態 |
|---|---|
| Text Edit / Save | ✅ 実装済み `[実コード: FilePreviewPanel.swift:1302, MarkdownPanel.swift:307 の saveTextContent()]` |
| Undo / Redo | ✅ 実装済み |
| Cmd+F / Replace | ✅ 実装済み |
| File Watch / 外部変更検知 | ✅ 実装済み `[実コード: MarkdownPanel.swift:429-435 の FileWatcher]` |
| 未保存バッファの管理 | ✅ `isDirty` 追跡あり `[実コード: FilePreviewPanel.swift:985]` |
| **Syntax Highlight** | ❌ **未実装（実質ここだけが差分）** |
| **Line Number** | ❌ **未実装** |

したがって本計画は「Editor を新規に作る」話ではなく、**既存 `FilePreviewTextEditor` を拡張する**話である。

### 結論2: upstream PR 戦略は成立しない

`[GitHub API]` upstream `manaflow-ai/cmux` は 2026-01-28 作成、Star 26,097 / Fork 2,217。PR 実績は **open 2,518件 / merged 3,571件 / closed 4,340件**。

- タイトルに editor を含む PR は **20件以上が open で、新規エディタ機能のマージ実績はゼロ**
- merged 側で editor を含むのは6件だけで、**全部が既存機能のバグ修正**（TextKit 1 強制 #5257、矢印キー移動と word-wrap #5247 など）

open PR が2,518件ある環境では PR を出すこと自体の期待値が低い。§39 は削るか大幅に弱めるべき。

### 結論3: fork はまだ素の状態

`[GitHub API]` `takashi11171117/cmux` は **独自コミット0 / upstream から12コミット遅れ**。着手前に upstream 追従が必要。

### 先に決めるべき設計判断

**新しい PanelType を足すか、既存 Panel を拡張するか。**

> ⚠️ **Claude の初回検証に誤りがあったため訂正（2回目の検証で判明）**
> 初回に「Surface が Session Restore の永続化単位」と書いたが**誤り**。実際は:
> - **Session Restore の永続化単位は `PanelType`** `[実コード: Sources/SessionPersistence.swift:1689-1692 の SessionPanelSnapshot.type]`。`PanelType` は既に **15種**（terminal / browser / markdown / filePreview / rightSidebarTool / customSidebar / simulator / agentSession / project / extensionBrowser / workspaceTodo / notifications / cloudVMLoading / mobilePairing / accountSignIn）`[実コード: Sources/Panels/Panel.swift:6-21]`
> - **`CmuxSurfaceType`（3種）は設定ファイルの宣言的レイアウトスキーマ**で、参照は `Sources/CmuxConfig.swift` 内の2箇所のみ `[実コード: Sources/CmuxConfig.swift:1611,1620]`。Session Restore とは無関係

したがって論点は「Panel 層か Surface 層か」ではなく、**新しい `PanelType` case を追加するか、既存の `filePreview` / `markdown` パネルを拡張するか**。前者は `SessionPanelSnapshot` の後方互換に注意が要る。競合 PR #4801 は Surface 型を足す方式だが、これは設定スキーマ側の話 `[PR: 4801]`。

---

## 業務要求（REQ-xx）

> 本節は 02_要件定義.md からトレースするための番号付け専用の追記。以降の本文（§1 以降）は原文＋訂正のまま変更していない。
> 出所タグ: `[メモ由来]` = §番号の原文または `> 🔴 訂正` ブロックに書かれている / `[要確認]` = 原文にあるが訂正で検証されていない。

### 方針・スコープ

| ID | 業務要求 | 出所 |
|---|---|---|
| REQ-01 | Claude Code / Codex 等の Agent を主役に置き、人間は必要な時だけ成果物・コード・画像・Markdown を確認する「Agent-first IDE」にする。VS Code の完全代替は目標としない。 | `[メモ由来: §1, §41]` |
| REQ-02 | cmux を fork して土台にし、Terminal / Browser / Workspace / Split / File Explorer / Markdown Viewer / File Preview / Diff Viewer / Session Persistence / Socket・CLI を再実装しない。 | `[メモ由来: §3.1 + 訂正]` |
| REQ-03 | 人間の編集用途は数行修正・typo 修正・JSON 修正・設定変更・Agent 成果物確認に限定し、大量編集は Agent または外部 IDE に任せる。 | `[メモ由来: §24]` |
| REQ-04 | 常時表示の File Tree / Outline / SCM / Problems / MiniMap を作らず、必要になった時だけ Inspector を出す UI にする。 | `[メモ由来: §9, §10, §26]` |

### 残差分（実際に作るもの）

| ID | 業務要求 | 出所 |
|---|---|---|
| REQ-05 | 既存 `FilePreviewTextEditor` に Syntax Highlight を追加する（新規エディタは作らない）。 | `[メモ由来: §0 結論1, §4 訂正, §13 訂正]` |
| REQ-06 | 同じエディタに行番号表示を追加する。 | `[メモ由来: §0 結論1, §4 訂正, §13 訂正]` |
| REQ-07 | Syntax Highlight ライブラリは maintenance 状況 / dependency 固定 / performance / license の4観点で評価してから選定する。選定が終わるまで工数見積もりを出さない。 | `[メモ由来: §14, §28 訂正]` |
| REQ-08 | Syntax Highlight は Swift / TypeScript / JavaScript / Dart / PHP / Python / JSON / YAML / Markdown / C / C++ / Rust / Go を最低限サポートする。 | `[メモ由来: §14]` |
| REQ-09 | 外部変更を検出したとき人間の編集中内容を勝手に消さず、Reload / Compare / Keep Mine を選ばせる。これは MVP 必須であり、検知機構は既存のため残作業は選択 UI。 | `[メモ由来: §35 + 訂正]` |
| REQ-10 | ファイルを開く処理を一箇所に集約し、拡張子で表示先を振り分ける。新規ルーターではなく既存ルーターの拡張として行う。 | `[メモ由来: §7, §8 + 訂正]` |
| REQ-11 | 外部 IDE（VS Code / Cursor / Xcode）連携で現在ファイルに加えて行番号まで渡す。アプリ起動自体は既存だが、**行番号を渡せる経路（`PreferredEditorService`）と行番号を持つ入口（エディタのヘッダメニュー）が現状交わっていない**ため、経路の接続そのものが残作業になる。成立しなければ MVP から外してよい。 | `[メモ由来: §25 + 訂正（2026-08-17 再検証で補正）]` |
| REQ-12 | Claude 専用 API を組まず、`cmux open <path>` のような通常の CLI から Viewer を開ける状態を保つ。Agent の成果物を自動で全部開くことはしない。 | `[メモ由来: §19, §20]` |

### 維持すべき制約（回帰させない）

| ID | 業務要求 | 出所 |
|---|---|---|
| REQ-13 | Editor 拡張によって Session Restore を不安定化させない。これを最優先の制約とする。 | `[メモ由来: §18, §30, §34-5]` |
| REQ-14 | cmux の軽さを失わない。Editor 未使用時はほぼ追加負荷なし / File Watch は Workspace 単位 / Syntax Highlight は visible range 優先 / Diff 更新は debounce / Image decode は非同期。 | `[メモ由来: §37, §34-2]` |
| REQ-15 | 数十MB 以上の log / binary / large generated file を Editor にそのまま読み込まない。サイズ判定で制限する。 | `[メモ由来: §36, §34-4]` |
| REQ-16 | fork 独自の変更は `docs/fork/` と専用ブランチに閉じ、upstream の Terminal / Workspace 構造を大きく変更しない。目的は upstream 追従コストの低減。 | `[メモ由来: 冒頭注記, §38, §39 訂正]` |
| REQ-17 | upstream への PR 還元を前提にしない。還元前提で差分を制約するコストは払わない。 | `[メモ由来: §0 結論2, §39 訂正]` |
| REQ-18 | 着手前に upstream 追従を済ませる（fork は独自コミット0 / 12コミット遅れ）。 | `[メモ由来: §0 結論3, §27 訂正]` |

### 先に決める・調べる

| ID | 業務要求 | 出所 |
|---|---|---|
| REQ-19 | Panel 層に足すか Surface 層に足すかを実装前に決める。Session Restore への影響が変わるため他の判断より先に決める。 | `[メモ由来: §0 先に決めるべき設計判断, §6 訂正]` |
| REQ-20 | Phase 0 の成果物として変更箇所マップを作る。既存 `FilePreviewTextEditor` の読解、競合4系統（#1909 / #4801 / #5638 / #2864）の方式比較、Diff Viewer の URL スキーム方式の読解、既存 Diff Viewer の動作確認、Syntax Highlight ライブラリ選定を含める。 | `[メモ由来: §27 訂正, §28 訂正, §40 訂正]` |
| REQ-21 | Image Preview の残論点は2点のみ。(1) **背景切替**を足すか（唯一の未実装操作）、(2) **SVG が実際に表示されるか**（`.image` モードには落ちるが `NSImage` の描画可否が未検証）。Fit / 100% / Zoom / Pan は実装済みのため確認不要。 | `[メモ由来: §12 訂正（2026-08-17 再検証で全面差し替え）]` |
| REQ-22 | Inspector のショートカット（⌘⇧E / ⌘⇧P / ⌘⇧D）と既存ショートカットの衝突を確認する。 | `[要確認: §10（訂正なし・未検証）]` |

### MVP 完成条件・後続

| ID | 業務要求 | 出所 |
|---|---|---|
| REQ-23 | MVP は次を全部満たしたときに完成とする: 複数 Claude Code 運用 / File Tree 表示 / Markdown 確認 / 画像確認 / syntax highlight 付きコード確認 / 数行修正 / Git diff 確認 / 再起動後の workspace 構成復元。未達は syntax highlight 部分のみ。 | `[メモ由来: §40 + 訂正]` |
| REQ-24 | Live Diff は既存の `DiffViewerLiveHTTPSession` の拡張として扱い、debounce の追加と更新トリガの接続を実作業とする。MVP 対象外の後続フェーズ。 | `[メモ由来: §17 訂正, §32]` |
| REQ-25 | LSP は MVP に入れない。ただしアーキテクチャ上あとから足せる形にする。 | `[メモ由来: §15, §33]` |
| REQ-26 | Git Worktree ごとの認識は MVP 対象外。 | `[メモ由来: §23]` |
| REQ-27 | IDE 型の「Project を開く」概念を強制せず cwd を基準にする。File Explorer root は current workspace cwd とし、任意の root 変更は許可する。 | `[メモ由来: §21]` |

---

# cmux + Warp系IDE機能 実装指針 v0.1

## 1. 目的

cmuxの強みである

- Workspace / Session管理
- Terminal
- Browser Pane
- Session Restore
- 複数Claude Code運用

を維持したまま、Warpで便利だった

- File Tree
- Markdown Preview
- Image Preview
- Diff Viewer
- Code Preview / 軽量Editor

を統合する。

目標はVS Codeの完全代替ではない。

**Claude Code / Codex等のAgentを主役にし、人間は必要な時だけ成果物・コード・画像・Markdownを確認できる「Agent-first IDE」**を作る。

---

## 2. 最終イメージ

```text
┌───────────────┬──────────────────────────┬────────────────────┐
│ Workspaces    │ Claude / Terminal        │ Files / Preview    │
│               │                          │                    │
│ DiveCut       │ > 実装しています...     │ src/               │
│ VisualPark    │                          │ ├ components/      │
│ Client A      │                          │ ├ lib/             │
│ Client B      │                          │ └ app/             │
│               │                          │                    │
│               │                          │ README.md          │
│               │                          │ output.png         │
└───────────────┴──────────────────────────┴────────────────────┘
```

普段は中央のAgentのみを見る。

必要な時だけ右側で、

```text
.md
→ Markdown Preview

.png / jpg
→ Image Preview

.swift / ts / dart / php
→ Code Viewer / Editor

diff
→ Diff Viewer
```

を表示する。

---

## 3. 基本方針

### 3.1 cmuxを土台にする

新規Terminalアプリは作らない。

cmux本体をforkし、既存機能を最大限利用する。

cmux側に既に存在する機能：

- Terminal Pane
- Browser Pane
- Workspace
- Split Pane
- File Explorer
- Markdown Viewer
- File Preview
- Diff Viewer
- Session Persistence
- Socket / CLI操作

これらを再実装しない。

> 🔴 **訂正: 実在確認の結果**
> - ✅ 実在: Terminal / Browser / Workspace / Project の各 Panel `[実コード: Sources/Panels/]`、File Explorer（約29ファイル）、MarkdownPanel、FilePreviewPanel、SessionPersistence
> - ⚠️ **Diff Viewer は Panel ではない** — URL スキーム + ブラウザ描画 `[実コード: Sources/Panels/CmuxDiffViewerURLSchemeHandler.swift, Sources/Panels/DiffViewerLiveHTTPSession.swift]`（§16 参照）。**再検証(2026-08-17)**: 初出時に `Sources/` 直下と書いたが誤り。実在は `Sources/Panels/` 配下で、§16・§17 の表記に統一した
> - ⚠️ **File Explorer のパスが違う** — `Sources/FileBrowser/` ではなく `Sources/FileExplorer*.swift` にフラット配置
> - ➕ 原文が挙げていないが既存: **テキスト編集と保存**（§4 参照）

---

## 4. 最初に追加するもの

> 🔴 **訂正: この節の前提が実態と異なる**
> テキスト編集・保存・Undo/Redo・Cmd+F/Replace・File Watch・外部変更検知は **すべて実装済み** `[実コード: Sources/Panels/FilePreviewTextEditor.swift]`。
> **未実装なのは Syntax Highlight と Line Number の2つだけ**。したがって「Editor を追加する」ではなく **「既存 `FilePreviewTextEditor` に Syntax Highlight と行番号を足す」** が正しい立て方。
> 下の「必要」リストのうち実際に必要なのは Syntax Highlight と Line Number のみ。

最優先は**Code Viewer / Lightweight Editor**。

不要：

- VS Code Extension API
- Debugger
- Full IntelliSense
- Terminal統合
- SCM UI全部
- Extension Marketplace

必要：

- Syntax Highlight
- Text Edit
- Save
- Search
- Line Number
- Basic Find/Replace
- File Watch
- External Modification Detection

まずここまで。

---

## 5. 参考実装

既存のcmux PRで実装されているText Editor系コードを参考にする。

> 🔴 **訂正: 該当 PR は #1909、ただし CONFLICTING**
> 下記ファイルパスの出典は **PR #1909「Add file browser drawer and text editor panel」（tayl0r）** `[PR: 1909]`。3ファイルとも完全一致する。
> - 状態: **`mergeable: CONFLICTING` / `mergeState: DIRTY`** — main と競合しており cherry-pick 不可
> - 2026-03-21 作成 / 最終更新 2026-07-15 / +1,813 -17（16ファイル）。**5か月 open のまま**
> - コミュニティから「main に入れてほしい」というコメントが複数付いているが動いていない
>
> **エディタ実装の open PR は4系統に分裂しており、原文は #1909 を暗黙に選んでいる。**比較検討が必要:
>
> | PR | 方式 | 規模 |
> |---|---|---|
> | #1909 | NSTextView（原文が採用） | +1,813 / 16ファイル |
> | #4801 | `CmuxSurfaceType` に code editor を追加 | +3,088 / 43ファイル |
> | #5638 | Monaco（WebView） | +903 / 69ファイル |
> | #2864 | 独自 EditorPanel | +57,839 / 124ファイル |
>
> なお下記のうち **`Sources/FileBrowser/FileBrowserView.swift` は PR 側のパス**であり、HEAD の File Explorer は `Sources/FileExplorer*.swift`（フラット配置）。移植時に読み替えが要る。

確認対象：

```text
Sources/Panels/TextEditorPanel.swift
Sources/Panels/TextEditorPanelView.swift
Sources/FileBrowser/FileBrowserView.swift
Sources/Workspace.swift
Sources/SessionPersistence.swift
Sources/AppDelegate.swift
```

このPRをそのままmergeするのではなく、

1. 現在HEADとの差分確認
2. UI構造確認
3. Panel Lifecycle確認
4. Session Restoreとの統合確認
5. 既知バグを修正

した上で移植する。

---

## 6. Panel設計

cmux既存のPanel abstractionに合わせる。

想定：

```text
Panel
├ TerminalPanel
├ BrowserPanel
├ MarkdownPanel
├ FilePreviewPanel
├ DiffPanel
└ CodeEditorPanel
```

Code Editorだけ特別扱いせず、既存Panelと同じLifecycleを持たせる。

> 🔴 **訂正: 先に「新しい `PanelType` を足すか、既存 Panel を拡張するか」を決める必要がある**
>
> ⚠️ **再検証(2026-08-17)で、この節の初回訂正に誤りがあったため差し替えた。** 初回訂正は「Panel と Surface は別レイヤーで、**Surface が Session Restore の永続化単位**」と書いていたが**誤り**である（§0 の ⚠️ で既に撤回済みだが、本節の本文が旧記述のまま残っていた）。
>
> 実コードでの事実:
> - **Session Restore の永続化単位は `SessionPanelSnapshot` で、種別は `PanelType` が持つ** `[実コード: Sources/SessionPersistence.swift:1689-1692]`
> - **`CmuxSurfaceType`（`terminal` / `browser` / `project` の3種）は cmux 設定ファイルの宣言的レイアウトスキーマ**であり、リポジトリ全体での参照は `Sources/CmuxConfig.swift:1611, 1620` の2箇所のみ `[実コード: grep -rn CmuxSurfaceType --include=*.swift . の全件]`。**Session Restore の経路には現れない**
>
> したがって論点は「Panel 層か Surface 層か」ではなく、**新しい `PanelType` case を追加するか、既存の `.filePreview` / `.markdown` を拡張するか**である。前者は `SessionPanelSnapshot` の後方互換に注意が要る。`CmuxSurfaceType` にケースを足すかどうかは「設定ファイルから宣言的にエディタを開けるようにするか」という別軸の話で、競合 PR #4801 はそちら側である `[PR: 4801]`。
>
> また上記 Panel 一覧の **DiffPanel は実在しない**（§16 参照）。

---

## 7. File Explorerとの連携

File Explorer上でファイルクリック時に拡張子でViewerを振り分ける。

例：

```text
.md
.markdown
→ MarkdownPanel

.png
.jpg
.jpeg
.webp
.gif
→ FilePreviewPanel

.swift
.ts
.tsx
.js
.dart
.php
.py
.rs
.go
.cpp
.h
.json
.yaml
.yml
.toml
→ CodeEditorPanel

その他
→ Generic File Preview
```

---

## 8. Open Routing

ファイルを開く処理を一箇所へ集約する。

```text
openFile(url)
 ↓
detectFileType()
 ↓
routeToPanel()
```

File Explorer、CLI、Agent、Drag & Dropのどこから開いても同じ処理を通す。

> 🔴 **訂正: Open Routing は既に存在する**
> `[実コード: Sources/CommandClickFileOpenRouter.swift]` と `[実コード: Sources/FileExplorerDoubleClickActionSettings.swift:11-27]`（`preview` / `defaultEditor` / `preferredEditor` の3分岐）が実装済み。
> **新規作成ではなく既存ルーターの拡張**として書くこと。
>
> ⚠️ **初回検証の誤りを訂正**: 「現状は外部アプリ起動に倒れている」と書いたが**誤り**。ダブルクリックの既定値は **`.preview`（内蔵プレビュー）** `[実コード: Sources/FileExplorerDoubleClickActionSettings.swift:33]`。外部アプリ起動は `defaultEditor` / `preferredEditor` を選んだ場合のみ。したがって内部表示への経路は既に既定で通っており、差分は**拡張子ごとの振り分け先の拡充**にとどまる。

---

## 9. 右ペイン優先

Agent-firstなので、ファイルを開くたび新規Workspaceを増やさない。

基本は、

```text
Workspace
├ Main Agent Pane
└ Inspector Pane
```

にする。

Inspector Pane内で、

- Code
- Markdown
- Image
- Diff

を切り替える。

理想的にはAgent Paneを維持したまま表示。

---

## 10. Inspector概念

将来的には独立した「Inspector」を作る。

```text
Inspector
├ Files
├ Preview
├ Diff
└ Code
```

普段は閉じる。

ショートカット例：

```text
⌘⇧E
Files

⌘⇧P
Preview

⌘⇧D
Diff
```

既存ショートカットとの衝突は確認する。

---

## 11. Markdown

既存Markdown Viewerを利用。

> 🔴 **訂正: 下記の必要条件はすべて実装済み**
> Live Reload は `FileWatcher` で実装済み `[実コード: Sources/Panels/MarkdownPanel.swift:429-435]`。Mermaid も対応済み `[実コード: Sources/Panels/MarkdownWebRenderer.swift, cmuxTests/MarkdownMermaidZoomTests.swift]`。
> **この節に着手すべき作業はない。**

必要条件：

- Live Reload
- GitHub Flavored Markdown
- Code Block
- Table
- Image
- Link
- Mermaid

Agentが `.md` を変更した際、表示中なら自動更新。

用途：

- 要求定義
- README
- AI生成仕様書
- 設計書
- TODO
- Report

---

## 12. Image Preview

既存File Previewを利用。

> 🔴 **訂正: 画像は QuickLook ではなく専用モードで描画され、操作もほぼ実装済み**
>
> ⚠️ **再検証(2026-08-17)で、この節の初回訂正に誤りがあったため差し替えた。** 初回訂正は「`FilePreviewPanel` は QuickLook 依存」「対応形式と操作は QuickLook の提供範囲次第。**未検証**」と書いていたが**誤り**である。
>
> 実コードでの事実:
> - 画像は `UTType.conforms(to: .image)` の判定で **`.image` モード**に解決される `[実コード: Sources/Panels/FilePreviewPanel.swift:768-781 の mediaMode(for:)]`。`.quickLook` は「テキストにもメディアにも解決できなかったファイル」の受け皿であって、画像の経路ではない `[実コード: 同:676-694]`
> - 描画は独自実装である。`FilePreviewImageContainerView` / `FilePreviewImageScrollView: NSScrollView` `[実コード: Sources/Panels/FilePreviewImageSession.swift:4-6, Sources/Panels/FilePreviewPanel.swift:4168]`
> - **下記「操作」のうち Fit / 100% / Zoom / Pan は実装済み**。専用クロームに Zoom Out / Actual Size / Zoom In / Zoom to Fit / Rotate Left / Rotate Right が並び、いずれもローカライズ済み `[実コード: Sources/Panels/FilePreviewPanel.swift:3631-3681]`。ピンチズームもある `[実コード: 同:3898, 3911]`。Pan は `NSScrollView` が担う
> - **未実装なのは背景切替のみ**（`checkerboard` / 背景トグルに相当する実装は `Sources/Panels/` に存在しない）
> - SVG は `public.svg-image` が `public.image` に conform するため `.image` モードには落ちる。**ただし `NSImage` が実際に描画できるかは未検証** `[要確認]`
>
> したがって残論点は「**背景切替を足すか**」と「**SVG が実際に表示されるか**」の2点であって、QuickLook の提供範囲の話ではない。

必要：

- PNG
- JPEG
- WebP
- GIF
- SVG

操作：

- Fit
- 100%
- Zoom
- Pan
- Background切替

特にAI生成物やスクリーンショット確認を重視する。

---

## 13. Code Viewer / Editor

> 🔴 **訂正: 下記 MVP のうち未実装は Syntax Highlight と Line Number のみ**
> Text Selection / Copy/Paste / Undo/Redo / Save / Cmd+F / Replace / File Watch / External Change Reload は **すべて実装済み** `[実コード: Sources/Panels/FilePreviewTextEditor.swift, FilePreviewPanel.swift:1302, MarkdownPanel.swift:307]`。

### MVP

- Syntax Highlight
- Line Number
- Text Selection
- Copy/Paste
- Undo/Redo
- Save
- Cmd+F
- Replace
- File Watch
- External Change Reload

### 後回し

- Autocomplete
- Code Action
- Rename Symbol
- Refactor
- Debugging
- Breakpoint

Claudeが主編集者なので、このへんは初期不要。

---

## 14. Syntax Highlight

> 🔴 **訂正: エンジン選定は AFIDE-01 で決着済み（同梱 highlight.js + JavaScriptCore を採用）**
>
> ⚠️ **再検証(2026-08-17)で、この節の初回訂正に誤りがあったため差し替えた。** 初回訂正は「`platforms` に `.macOS(.v14)` が含まれるため macOS 側からも利用できる可能性が高い」「**ライブラリ選定は既存 Highlightr を流用できるかから始めること**」と書いていたが、**この誘導は誤りだった**。
>
> 実コードでの事実:
> - **macOS アプリ本体は `CmuxAgentChatUI` を一切リンクしていない**（`cmux.xcodeproj/project.pbxproj` に出現0件）`[実コード]`
> - **macOS 側の `Package.resolved` に Highlightr は無い**（pin 14件中0件）`[実コード: cmux.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved]`
> - `Package.swift` の `platforms` 宣言は**リンクの有無とは無関係**である。`.macOS(.v14)` があることは「macOS 本体から使える」ことを意味しない
>
> **決定（AFIDE-01 の実測調査による。→ [`issues/afide-01_調査結果.md`](./issues/afide-01_調査結果.md)）**: 採用は **選択肢 C = 同梱済み `Resources/markdown-viewer/highlight.min.js` を JavaScriptCore で直接評価**（SPM 依存ゼロ / `Package.resolved` 差分ゼロ / バンドル増分ゼロ）。
>
> Highlightr を却下した理由:
> - **upstream 自身が README で「2026年以降メンテナンスしていない」と宣言している**（2026-02-13 の "Added Disclaimer" コミット）
> - CSS パーサの欠陥で **`github` テーマは文字列が、`github-dark` テーマはキーワードが本文色のまま**になり、FR-01 の受け入れ条件を満たさない
> - 実測で C より 1.4〜1.5 倍遅い
>
> なお本節の原文が挙げる「TextMate Grammar / Tree-sitter」も、AFIDE-01 の3案（Highlightr / 自前スキャナ / 同梱 highlight.js）には含めなかった。**初回訂正が Highlightr へ誘導した結果、最終的に採用された選択肢 C は 03_詳細設計 §5 D-2 が独自に発見するまで視野の外にあった。**

まずTextMate Grammar / Tree-sitter等を検討。

重要条件：

- Swift
- TypeScript
- JavaScript
- Dart
- PHP
- Python
- JSON
- YAML
- Markdown
- C/C++
- Rust
- Go

を最低限サポート。

既存PRが使用しているライブラリをまず評価し、

- maintenance状況
- dependency固定
- performance
- license

を確認する。

---

## 15. LSP

MVPには入れない。

ただしArchitecture上は後付け可能にする。

将来：

```text
CodeEditorPanel
      ↓
LSPClient
      ↓
Language Server
```

追加候補：

- diagnostics
- go to definition
- find references
- hover
- symbol search

最初からVS Code化しない。

---

## 16. Diff

既存Diff Viewerを利用。

> 🔴 **訂正: Diff は Panel ではなくブラウザ描画**
> 実装は URL スキームハンドラ + ローカル HTTP セッション。
> ⚠️ **初回検証のパス表記を訂正**: 中核は `Packages/macOS/CmuxBrowser/Sources/CmuxBrowser/DiffViewer/` 配下（`CmuxDiffViewerPreparedSession.swift` 等）と `Sources/Panels/` 配下、CLI 側は `CLI/CMUXCLI+TypedDiffViewer.swift` 等に分散している `[実コード]`。Phase 0 の調査対象を実パスに直すこと。
> `Sources/Panels/` に DiffPanel は存在しない。§6 の Panel 一覧に DiffPanel を並べるなら、**既存の URL スキーム方式に乗るのか Panel として作り直すのかで工数が変わる**ため、方針を決めること。

Agent利用では非常に重要。

理想フロー：

```text
Claudeが変更
↓
Git変更検知
↓
Diffタブ更新
↓
ユーザー確認
```

将来的にはWorkspace単位で

```text
Modified Files (4)
```

を表示。

クリックでDiff。

---

## 17. Live Diff

優先度高めの追加候補。

> 🔴 **訂正: 新規実装ではなく既存の拡張**
> `DiffViewerLiveHTTPSession` が既に存在する `[実コード: Sources/Panels/DiffViewerLiveHTTPSession.swift]`。ライブ更新の土台はあるので、debounce の追加と更新トリガの接続が実作業になる。

Claude Codeが編集中でも表示を更新する。

```text
Agent modifies file
↓
Filesystem watcher
↓
git diff更新
↓
Diff Panel refresh
```

ただし編集途中の大量更新でCPUを消費しないようdebounceする。

---

## 18. Session Restore

cmuxの最大の強みなので壊さない。

再起動後、

```text
Workspace
├ Claude Tab
├ cwd
├ pane layout
├ Inspector state
└ opened file
```

まで戻す。

Claudeプロセス自体の復元はcmux/Claude側の仕様に従う。

Editor追加でSession Restoreを不安定化させないことを最優先する。

---

## 19. Claude Codeとの連携

> 🔴 **訂正: `cmux open` は既に実装済み**
> `cmux open <path-or-url>` が CLI に存在する `[実コード: CLI/cmux.swift:18002,18397 / docs/cli-contract.md:567]`。
> したがって「Viewer を開ける状態を目指す」のではなく、**既に目標状態にある**。残るのは開いた先の振り分け（§8）だけ。

初期はClaude専用APIを組まない。

Claudeから普通に、

```bash
cmux open README.md
```

や、

```bash
cmux open output.png
```

のようにViewerを開ける状態を目指す。

最終的にはClaudeに、

> 作成した画像を開いて

と言えばcmux CLIを呼べる。

---

## 20. Agent-aware Open

将来的にはAgent Hookを利用し、

Claudeが

```text
Created:
docs/design.md
```

のような成果物を出した際、

ユーザー操作で即Preview可能にする。

勝手に全部開くと鬱陶しいので、自動Openは慎重にする。

---

## 21. WorkspaceとRepository

IDE型の「Projectを開く」概念を強制しない。

cmuxらしく、

**cwdを基準**

にする。

File Explorer Root：

```text
current workspace cwd
```

必要なら任意のroot変更を許可。

---

## 22. 複数Agent

同一repo内でも複数Agent sessionを並列表示できる。

```text
VisualPark
├ Claude A
├ Claude B
├ Codex
└ Inspector
```

Workspace / Surface管理はcmux既存機能を使う。

---

## 23. Git Worktree

将来的にはAgentごとのworktree認識を追加すると強い。

```text
VisualPark
├ main
├ feature-a
└ bugfix-b
```

File Explorer rootも各worktreeに追従。

ただしMVP対象外。

---

## 24. Code Editingの位置づけ

重要：

**人間がメインでコードを書くIDEにはしない。**

目的は、

- 数行修正
- typo修正
- JSON修正
- 設定変更
- Agent成果物確認

程度。

大量編集はClaude/Codexまたは外部IDEへ任せる。

---

## 25. External IDE連携

> 🔴 **訂正: 外部エディタ連携は既に存在する。ただし「行番号の受け渡しのみ」は過小評価**
> `[実コード: Sources/FileExplorerExternalOpenMenuItems.swift]` が `FileExternalOpenApplicationResolver` を使い、LaunchServices ベースで「このアプリで開く / 他のアプリで開く」を実装済み。
>
> ⚠️ **再検証(2026-08-17)による補足**: 初回訂正は「残差分は行番号の受け渡しのみ」としていたが、**実際には構造的なギャップがある**（03_詳細設計 §10.2 で判明）。
> - 外部起動の経路は2系統ある。`FileExternalOpenAction`（LaunchServices）と `PreferredEditorService`（コマンド実行）
> - **行番号を渡せるのは `PreferredEditorService` 側だけ**だが、その呼び出し元（File Explorer のダブルクリック、ターミナル Cmd-click）は**カーソル行を持たない**
> - **カーソル行を持つのは File Preview / Markdown のエディタだけ**だが、そのヘッダメニューは `FileExternalOpenAction`（LaunchServices）にしか繋がっていない
> - つまり「**行番号を渡せる経路**」と「**行番号を持っている入口**」が現状**交わっていない**。成立させるにはヘッダメニューへの経路追加と下位パッケージ `CmuxWorkspaces` の public API 追加という2つの許可が要る（03 新規-G）
>
> このため AFIDE-14 は「**実装して差分を見てから判断し、無理なら打ち切る**」issue として起こされている。REQ-11 は他の残差分より難度が高い。

それでも必要な時用に、

```text
Open in VS Code
Open in Cursor
Open in Xcode
```

は付ける。

現在ファイル / 行番号まで渡せるとよい。

つまり、

**普段はcmux**
**本気で編集するときだけ外部IDE**

にする。

---

## 26. UIの思想

WarpのIDEをコピーしない。

cmuxのUI思想を保ちながら、

**必要なものだけ出る**

設計にする。

NG：

```text
常時File Tree
常時Outline
常時SCM
常時Problems
常時MiniMap
```

OK：

```text
Claude主体
↓
必要になった時だけInspector
```

---

## 27. Phase 0：コード調査

> 🔴 **訂正: 調査対象を実パスへ、追加項目あり**
> - fork は **独自コミット0 / upstream から12コミット遅れ** `[GitHub API]`。まず upstream 追従から始める
> - `Sources/FileBrowser/` は存在しない。File Explorer は `Sources/FileExplorer*.swift`
> - **既存の `FilePreviewTextEditor` の読解を最優先**（実質ここに足すため）`[実コード: Sources/Panels/FilePreviewTextEditor.swift]`
> - Text Editor PR は **#1909（CONFLICTING）**。競合4系統（#1909 / #4801 / #5638 / #2864）の方式比較も Phase 0 の成果物に含めること
> - Diff Viewer は Panel ではないので、URL スキーム方式の読解が必要

最初にやること。

- cmuxをclone
- 最新HEAD build
- Panel architecture確認
- File Explorer確認
- MarkdownPanel確認
- FilePreviewPanel確認
- Diff Viewer確認
- Session Persistence確認
- Text Editor PR解析

成果物：

**変更箇所マップ**

---

## 28. Phase 1：Editor PoC

目標：

```text
File Explorer
↓
.swift / .tsクリック
↓
CodeEditorPanel
↓
Syntax Highlight
↓
編集
↓
保存
```

これだけ。

期間想定：

数日。

> 🔴 **訂正: スコープと見積もりの根拠**
> 上記フローのうち **File Explorer → クリック → Panel 表示 → 編集 → 保存は既に動く**。Phase 1 の実作業は **Syntax Highlight の載せ込みと行番号表示**に絞られる。
> ただし「数日」の根拠は無い。§14 で Syntax Highlight ライブラリを maintenance / license / performance で評価すると書き、§34-1 でその品質をリスクに挙げている以上、**ライブラリ選定が終わるまで工数は出せない**。選定を Phase 0 の成果物に含め、見積もりはその後に置くこと。

---

## 29. Phase 2：Preview統合

Markdown / Image / Code / Diffを同じInspector workflowへ統合。

```text
File Tree
↓
クリック
↓
適切なViewer
```

Agent paneは維持。

---

## 30. Phase 3：Session Restore

再起動して、

- File Explorer root
- Inspector open/closed
- 表示中ファイル
- pane size

が戻ることを確認。

---

## 31. Phase 4：Agent Workflow

実際にClaude Codeを使う。

テスト例：

```text
ClaudeにREADMEを書かせる
↓
Markdown Preview

Claudeに画像を書き出させる
↓
Image Preview

Claudeにコード変更させる
↓
Diff

数行だけ手修正
↓
Code Editor
```

この一連が快適か評価。

---

## 32. Phase 5：Live Diff

必要なら追加。

Claude編集をリアルタイムでDiff Viewerへ反映。

---

## 33. Phase 6：LSP

実際に日常利用して、

「定義ジャンプ無いのがしんどい」

となってから入れる。

必要性が無ければ実装しない。

---

## 34. 技術リスク

### 1. Editor dependency
Syntax Highlightライブラリの品質。

### 2. File Watch
AIが短時間に大量編集するためイベント数が多い。

### 3. Save conflict
Claudeが同じファイルを編集している最中に人間が保存するケース。

### 4. Large File
巨大JSON、log等をEditorで開かない。

### 5. Session Persistence
Editor状態追加によって既存restoreを壊さない。

---

## 35. Conflict対策

外部変更を検出した場合、

勝手に人間の編集中内容を消さない。

```text
File changed externally.

Reload
Compare
Keep Mine
```

のような処理を検討。

Agent-first IDEでは普通のIDE以上に重要。

> 🔴 **訂正: 「検討」ではなく MVP 必須。ただし土台は既にある**
> §13 の MVP に Save が入っており、§34-3 で保存衝突をリスクに挙げている以上、衝突時の動作は **MVP で決まっていなければならない**。
> 既存実装は `isDirty` を追跡し、外部変更時に未保存バッファを破棄せず再読込する `[実コード: Sources/Panels/FilePreviewPanel.swift:985,1176,1193 / MarkdownPanel.swift:271]`。
> したがって残作業は **Reload / Compare / Keep Mine の選択 UI** であり、検知機構の新規実装ではない。

---

## 36. 大容量ファイル

以下はEditorへそのまま読み込まない。

- 数十MB以上のlog
- binary
- large generated files

サイズ判定してPreview制限。

---

## 37. Performance

cmuxの軽さを失わない。

目標：

- Editorを開いていない時はほぼ追加負荷なし
- File WatchはWorkspace単位

> 🔴 **訂正: 現状の実装はパネル単位**
> MarkdownPanel は自身の `filePath` を個別に watch する `[実コード: Sources/Panels/MarkdownPanel.swift:429-435]`。Workspace 単位の watch は設定ファイル用に別途ある `[実コード: Sources/CmuxConfig.swift:3065 startLocalFileWatcher()]`。
> 「Workspace 単位にする」が**目標**なのか、現状のパネル単位で十分なのかを決めること。**未確定。**
- Syntax Highlightはvisible range優先
- Diff更新はdebounce
- Image decodeは非同期

---

## 38. Fork方針

最初は自社forkで実装。

ただしupstreamとの差分を小さく保つ。

変更は、

```text
CodeEditorPanel
FileOpenRouter
Inspector Integration
```

程度に集中。

cmux本体のTerminalやWorkspace構造を大きく変更しない。

---

## 39. Upstream PR

> 🔴 **訂正: この戦略は成立しない。削るか大幅に弱めること**
> `[GitHub API]` upstream の PR 実績は **open 2,518件 / merged 3,571件 / closed 4,340件**（2026-01-28 作成、Star 26,097 / Fork 2,217）。
> - タイトルに editor を含む PR は **20件以上が open で、新規エディタ機能のマージ実績はゼロ**
> - merged 側で editor を含むのは6件のみで、**全部が既存機能のバグ修正**（#5257 TextKit 1 強制、#5247 矢印キー移動と word-wrap、#6956 IME 切替 など）
>
> open PR が2,518件ある環境では PR を出すこと自体の期待値が低い。**upstream 還元を前提に差分を制約するコストに見合わない。**
> ただし §38 の「upstream の Terminal / Workspace 構造を大きく変更しない」は、**追従コストを下げるという別の理由で有効**なので維持してよい。

完成度が上がったら、

- Code Editor
- File Routing
- Inspector改善

を機能単位でupstream PRすることも検討。

自社forkだけに巨大差分を抱えない。

---

## 40. MVP完成条件

> 🔴 **訂正: 大半は既に満たされている**
> 下記のうち未達は **「コードを syntax highlight 付きで確認できる」の syntax highlight 部分のみ**。
> なお「Git diff を確認できる」は §13 の MVP 機能リストに入っていない（§16 で既存利用の前提）。**既存 Diff Viewer が期待どおり動くかの確認を Phase 0 の調査項目に加えること。**

以下が全部できれば一旦完成。

```text
Claude Codeを複数運用できる

+

File Treeが見れる

+

Markdownをcmux内で確認できる

+

画像をcmux内で確認できる

+

コードをsyntax highlight付きで確認できる

+

数行なら修正できる

+

Git diffを確認できる

+

cmux再起動後もworkspace構成が戻る
```

---

## 41. 最終コンセプト

目指すものは、

**VS Codeの中にTerminalを置くことではない。**

逆に、

> **Agent / Terminalを中心に置き、必要なIDE機能だけを周辺に置く。**

という開発環境。

```text
Traditional IDE

Editor
├ Terminal
├ AI
└ Git
```

ではなく、

```text
Agent-first IDE

Agent / Terminal
├ Files
├ Preview
├ Diff
└ Lightweight Editor
```

にする。

cmuxのセッション管理とWarpの軽量IDE体験を組み合わせた、

**Claude Code / Codex時代の開発環境**

を目標とする。
